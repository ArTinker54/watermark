# Ставить строго с --no-deps, иначе подтянется opencv-python с GUI-библиотеками
# и перекроет opencv-python-headless из requirements.txt:
#
#   pip install --no-deps -r requirements-wm.txt
#
# Версия зафиксирована жёстко: blind-watermark требует совпадения версии
# при встраивании и извлечении метки.
blind-watermark==0.4.4
